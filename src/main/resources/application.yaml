spring:
  application:
    name: online-store
  datasource:
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: postgres
    url: jdbc:postgresql://localhost:5432/online-store
  jpa:
    generate-ddl: true
    database: postgresql
  mvc:
    hiddenmethod:
      filter:
        enabled: true
